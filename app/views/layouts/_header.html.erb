<nav class="navbar navbar-expand-lg navbar-light bg-light fixed-top">
  <div class="container-fluid nav-container">
    <a class="navbar-brand" href="<%= root_path %>">Loopview</a>
    <% if logged_in? %>
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link" aria-current="page" href="<%= projects_path %>">Projekte</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" aria-current="page" href="<%= issue_templates_path %>">Vorlagen</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="<%= new_user_path %>">Nutzer hinzufügen</a>
        </li>
      </ul>
      <div class="dropdown">
        <a href="#" class="d-flex align-items-center link-dark text-decoration-none dropdown-toggle" id="dropdownUser2" data-bs-toggle="dropdown" aria-expanded="false">
          <span class="far fa-user p-2"></span>
          <strong><%= current_user.full_name %></strong>
        </a>
        <ul class="dropdown-menu text-small shadow mr-5" aria-labelledby="dropdownUser2">
          <li><a class="dropdown-item" href="/logout">Sign out</a></li>
        </ul>
      </div>
    <% end %>
  </div>
</nav>

